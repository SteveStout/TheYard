# Changelog

One line per shipped version, newest first. The number is the one the page
footer shows, and 1.0.0.N is the Nth build that reached the live site. How a
line gets here, and why there is only ever one sentence, is recorded in
ADR: The changelog under the Decision Records index. From 1.0.0.41 the top
line here is where the deploy reads the version from, so this file and the page
footer cannot disagree (ADR: The version comes from the changelog). One number
is missing, 1.0.0.39, skipped under the older formula when a red build consumed
a deploy run number.

- **1.0.0.84** (2026-09-04): The inventory grid was asking the window how wide it was, and the window is the wrong thing to ask. Three columns arrived at 1024 pixels, which is the exact width the docked rail appears at, so the grid went to three columns at the moment it lost 272 pixels to the rail: cards about 215 wide, wrapping every title, every badge row and every city name, at a laptop and tablet width. It asks its own box now, through a container query, which also means a collapsed rail hands it two hundred pixels back and it uses them, something a media query cannot see. Two browser tests measure the card tops at 1024: two on the first row with the rail open, three with it collapsed. The threshold is 860 rather than 900 because a collapsed rail at that window leaves 912 pixels and a classic scrollbar takes fifteen of them, and a threshold a card's width away from the case it is meant to catch is one that catches it on some machines. Also the other two suite counts in the README, which said 43 Playwright and 48 Vitest against 52 and 57. The browser count is counted now, because every spec declares its tests the same way; the Vitest one is dated instead, because two of its rows are an it.each whose length is only known when the file runs, and a number nobody can check should say when it was true rather than pretend.
- **1.0.0.83** (2026-09-04): The preview card shipped with no version on it, and every check passed. It is regenerated at the start of a ship, from the changelog's top line, which is also where the deploy reads the version from; on that ship the changelog was momentarily empty when the generator read it, so it exited zero and drew a card with a blank where the version goes. Nothing noticed, because the record count on the card was right and the file was large enough to have rendered, and those were the only two things anybody had thought to assert. The card's version is held to the changelog's top line now, which is the same line the deploy trusts, and the card is redrawn. A generated number is worth nothing without a test, which is an argument this project had already made about a different number on the same image (ADR-053).
- **1.0.0.82** (2026-09-04): A vehicle page was printing "2.0 Rough" above "Vehicle shows average wear for its age and mileage. Mechanically sound." and "No damage reported.", which is three fields disagreeing about the same car on one screen. The seed dataset is careful about this: band 1 reads as salvage, band 2 as rough with damage, band 3 as average, band 4 and 5 as clean, and the damage note counts fall from about four to none across that range. The synthesiser that turns two hundred seeds into a hundred thousand records was drawing the condition grade from the hash across the whole scale while the report, the damage notes and the title status came from the seed untouched, so every correlation the data had was discarded for 99,800 of the 100,000. The grade varies inside its seed's band now, which keeps all of them and costs one line, and a theory holds five bands to it. And the README said 236 xUnit tests when there were 312, so that number is counted rather than typed from 1.0.0.81 and this version moves it to 318.
- **1.0.0.81** (2026-09-04): The em dash gate failed on itself, and the annotation added in 1.0.0.71 named it in one public line the first time it mattered. The scan went red on CI on a commit that changed a caption and a stylesheet, green locally and green on the four runs either side, which is the shape of a flake and was not one. The console logger CI asks for, so individual test names appear, prints each test with its parameters spelled out, and one of that class's own parameters is the em dash it forbids, because a rule that cannot fail is not a rule and the theory proving it can fail has to contain one. The job pipes that output through tee into a file in the checkout, so the transcript of the running suite held the character, sat in the repository the scan reads, and the scan read it; whether it failed depended on whether that line was flushed before the scan got there. Two things were wrong and only one was about em dashes. A build should not write into the source it is building, so the transcripts go to the runner's temp directory now, which fixes this and every future test that reads the repository, a growing list. And the scan skips those two names anyway, because the next person to add a tee will not remember why the first one moved. The local gate never saw it and never could: the ship script already writes its transcript outside the repository, so the gate that runs in two places was running against two different trees (ADR-052 addendum). The .NET annotation also carries the three lines under each marker now, because "Assert.Contains() Failure" says which assertion and the line below it says what was actually in the collection, and that second line is the one somebody needs. And the inventory says once, above the grid, that the photographs are stock chosen for the body style: fifty of them do the work of a hundred thousand, so two trucks in the same row can carry the same picture, which without a line saying so reads as a rendering fault.
- **1.0.0.80** (2026-09-04): 1.0.0.75 said the front page stays live-looking for as long as it is open. It was shipped, then measured, and that sentence was wrong. A browser left on the live site made five requests for the listing in seventy seconds, at 0, 15, 30, 46 and 61, on a tab it reported as visible, exactly as designed, and after seventy seconds the top four cards read "Ended Sep 4, 7:50 p.m." Both of those are true, which is the lesson: the mechanism did what it was built to do and the outcome it was built for did not follow, because the premise underneath it was never checked, that a fresh answer has a fresh first row. Over a hundred thousand auctions the soonest one ends inside a second, so the first row of every answer is expiring while it paints, and a shorter interval asks more often for a page with the same problem. What fixes it is the one thing the browser has and the response does not, which is the time now: under the ending-soonest sort the page already in hand is reordered on the browser's clock by the same ranking the server applied, live first and closest to ending, then upcoming, then ended. It is a reorder and not a filter, so nothing is added or dropped and the count and the paging stay the server's, and a page ranked by price or by bids is left exactly as it arrived. The refresh stays, because it is what brings new lots in as old ones leave and what the reorder runs on; neither is sufficient alone. The claim this time is the one that was measured: an auction that ends while the page is open moves out of the way within a second, without a request (ADR-056 correction).
- **1.0.0.79** (2026-09-04): Two things a reviewer would have drawn their own conclusion about. A screenshot of the detail page shows "2021 Tesla Model Y Performance" over a photograph of a Hyundai SUV, which is exactly what the design does on purpose: the catalogue is synthetic and the photographs are vendored stock chosen from the pool for the body style, which the README has said since the photos shipped. The README is not where somebody looking at that page is. A caption under the gallery says it now, because a reader who notices before being told it is deliberate has been handed a reason to doubt everything else on the page, and the fix is one sentence rather than fifty thousand photographs. And records got addresses one version ago with nothing on the page admitting it, which is a feature nobody has: the dialog carries a Copy link button beside Close, and when the browser refuses the clipboard, which happens on an insecure origin or a denied permission, the label says the link is in the address bar, which it is (ADR-057).
- **1.0.0.78** (2026-09-04): Fifty-seven decision records, and no way to send anybody one of them. The only route to a record was open the site, find the group, expand it, scroll, click, which is fine for somebody browsing and useless for every way a record actually gets read: cited in an interview, linked from another record, kept in a tab that gets reloaded, pasted into a message, which is how a portfolio travels. Every other view here already had an address, and the documents were the exception only because the dialog that shows them happened to be owned by the sidebar rather than by the page. They have one now, ?doc=adr-lockout, and the slug is read off the URL the API already serves the document at rather than written a second time, so a record cannot become linkable under a name the API does not answer to. An address that names nothing opens nothing and lands on the inventory, because a link that has gone stale should show a reader the thing it was attached to rather than a failure. Opening pushes a history entry the way opening a vehicle does, so Back closes a record; and since Escape, the X and the backdrop close it too, closing checks whether we pushed the entry that opened it, going back when we did so Back and Escape agree, and swapping the parameter out in place when we did not, which is what a visitor arriving on the link gets and is the difference between closing a dialog and leaving the site. The state moved up to the page, since the browser can now close a record without the dialog being touched, so the dialog follows the state instead of being it (ADR-057).
- **1.0.0.77** (2026-09-04): A third of the decision records index was arriving cut off. The rail is 270 pixels and the labels ellipsize, so the centre of this project read as "008 ADR: Linux over Wind...", "010 ADR: Observability (A...", "014 ADR: Live code sampl...", which is not a shorter row, it is a row a reader cannot tell from the one under it. Eight specs click those rows and every one of them passed the whole time, because each asks for a row by its full accessible name and the accessible name is complete: the ellipsis is painted, not stored. That gap has a shape, the model is right and the render is not, and no assertion about the model can reach it. So the labels wrap now, which costs a few pixels of height in a list that already scrolls because rows have a minimum height rather than a fixed one, and one test asks the browser instead of the DOM whether any span in the rail is wider than the box it was given. It fails against the rail as it was (ADR-056 addendum).
- **1.0.0.76** (2026-09-04): Four things a screenshot showed and nothing else would have. The Admin tab's timing summary read "Answers: 367 of 200, 133 of 404", which is two numbers with no relationship between them, on the page whose whole job is being readable by somebody who did not write it; it says "367 with status 200" now and a browser test holds the sentence. Recent errors was the fourth card in a three-track grid, so it sat alone on the second row with two thirds of the width empty, which reads as something failing to load rather than as a layout, and it takes the row now. The sign-in card is 560 pixels pinned to the left of a twelve-hundred-pixel canvas, which reads as a layout that ran out before it finished, and centred it reads as a sign-in page. And the sidebar was ellipsizing "Coding and commenting style" to "Coding and commenti...", which is a label shortened by the browser rather than by anybody who thought about it. None of these is a defect and all four of them are the first thing a reader sees, which is the argument for looking at the thing rather than at its tests (ADR-055).
- **1.0.0.75** (2026-09-04): Somebody took a screenshot of the front page and looked at it, which nothing in this project had ever done, and the first row was three cards reading "Live 7s left", "Live 7s left" and "Live 13s left". Fifteen seconds later the same vehicle at a phone width was a grey chip reading "Ended". Nothing was broken. The default sort is ending soonest, which is what an auction marketplace opens on and which over a hundred thousand lots always means seconds; the server ranks live ahead of ended, correctly; the browser recomputes each card's status as the clock ticks so a countdown reaching zero flips the chip without a refetch, on purpose. Together they mean the page is right at the instant it is answered and rots from the top for as long as anybody reads it, so a minute in, the first impression of a live auction site is a wall of dead lots that will not move. It is re-asked at the next moment its answer can change now, which is the soonest start or end still ahead of the vehicles on the page, floored at fifteen seconds so a page where something ends every few seconds asks four times a minute rather than constantly, skipped entirely while the tab is hidden and paid back when it returns, and asking nothing at all on a list whose auctions end next week, which the fixed timer it replaces could not manage. The timer that was there answered a different question, membership drifting under a status filter, and only ran when one was on (ADR-056). Also from the same look at the screenshots: the price filter on a phone had about sixty pixels an input, which clips the word "Max" inside its own box, and the search placeholder was one word longer than the field. And the record Steve asked for, on broken windows and the Boy Scout rule, with this week's seven findings as its evidence and the part usually left out included, which is that the rule taken literally produces unrelated changes inside a focused diff and the answer is that a window either gets fixed in place because it is small and on the subject, or becomes its own version. The half a machine can see is now three tests: no markers, no console call or debugger under src, and no focused or skipped test, with both runners refusing a focused test on CI and allowing one locally, since a committed test.only turns a suite into one test and reports it green (ADR-055).
- **1.0.0.74** (2026-09-03): Ask the narrow question instead of the broad one: what can somebody with no account cause this application to keep? Reads cost a query, a browser error report goes into a ring with fifty bounded slots, and registration writes a row to Azure SQL Database and keeps it. That is the whole list, and only the last one persists, which by the key on the bids table makes the number of accounts the bound on everything anybody can add to this database. It had no bound at all. The rows are the smaller half: Identity hashes a password on the way in on purpose, about 120 ms of CPU, on the one container that serves everything else, so a loop against this endpoint was the best attack this site exposed and it needed no account and no cleverness. The security page has said since it was written that a rate limiter here would not be the control that mattered, and every word of that argument is about partitioning: behind the edge the origin sees one address for everybody and the origin is directly reachable, so a per-address limit is a global cap being lied to. It says nothing about a limit that does not partition, which has nothing to forge. There is a sliding hour of 120 registrations now, counted across the whole site, taken after the request is read and before the password is hashed, sliding rather than resetting so nobody takes two allowances either side of an hour boundary, and a refusal takes no slot so that fifty people knocking cannot hold the window open forever. The refusal carries no digit in its words, because a reply that says how much is left is a counter anybody can poll. What it costs is in the record rather than hidden: while an attacker spends the hour, a real visitor cannot register either, and everything else keeps working (ADR-054).
- **1.0.0.73** (2026-09-03): Every record links at the code it decided about, and a test has held those links since 1.0.0.67. The code links back, by title, in a hundred and eleven comments, and nothing had ever checked one of them. Five were wrong: two named a record that does not exist, one cited a section heading as though it were a title, and two were near misses a reader would never catch, "Search index" for "The search index" and "Edge economics" for "Edge deploy economics". A wrong citation costs more than no citation, because it tells a reader there is a record to go and read, and the reader who goes and finds nothing concludes the documentation is decorative. Both directions are checked now, by title and by number, and a citation that names two records fails exactly like one that names none, since "The" begins twenty of these titles. One of the two missing records is written rather than renamed away, because it was owed: the public face shipped in 1.0.0.58 with no record and a test citing one by name for five versions, and ADR-053 now says why the structured data claims source code by a named author rather than the Organization that would unfurl better and be false, and why a generated number is worth nothing without a test. That last part earned itself: the count test was asking an easier question twice over. It read each file as written, so a claim the editor had wrapped across a line break sat invisible to it while it passed on the other one, and it read the README while How this was built carried the same count in a sentence of its own. It collapses whitespace first and reads every living document now, which means every served document that is not a record and not the changelog, since those two are dated by nature and quote counts that were true when they were written.
- **1.0.0.72** (2026-09-03): A rule this project has enforced for forty versions was being checked by a script on one laptop. Nothing written here contains an em dash, and the only thing holding the whole repository to that was a PowerShell loop in the shipping script: not on the runner, not runnable by anybody who clones this, skipped by any commit that does not go through it, while the single test that did enforce it read the changelog and nothing else. Every other gate in that script has a counterpart in CI, so this one was the exception without anybody having decided it should be, and the expensive part is that it looked enforced: every decision record, the README, the security page and every code comment the site displays as a live sample were covered by a sentence in a document and a loop one machine runs. It is a test now, so CI runs it, a clone runs it, and a violation names its file and its line instead of a path on somebody's C drive. It knows the character and the two HTML ways of writing it, which are plain ASCII and render as one em dash in served markdown, and it reads UTF-8 explicitly, because a reader that guesses the encoding splits one multi-byte character into two single-byte ones and then finds whatever it likes in the pieces, which is how the PowerShell version reported violations in files whose only crime was a multiplication sign. A second test asserts the scan reads something, since a scan pointed at the wrong directory passes in silence, which is the shape of defect the version before this one was also about (ADR-052).
- **1.0.0.71** (2026-09-03): The step that names a failing .NET test on the run page was half working, and the half that was not would have failed silently, which is the one weakness of building this out of pattern matches: a pattern that matches nothing leaves the step green, the annotation list empty and the run page saying "Process completed with exit code 1", which is exactly what it said before the work. dotnet writes its totals two different ways depending on which logger is asked for, either one "Passed! - Failed: 0, Passed: 281" line or a four-line "Test Run Failed. / Total tests:" block, and the pattern knew only the first while this job asks for the logger that prints the second. The browser suite had the same shape of gap from the other direction: it matched "Timeout of" and a test that runs out of time says "Test timeout of 90000ms exceeded.", which is the whole finding when the failure is a hang and no expectation was ever reached. Both are fixed, and the fix worth having is the one that holds them there: the patterns are read back out of ci.yml and run against real failing transcripts kept as fixtures, one from each suite, so a pattern that stops naming the failure goes red on a machine somebody is watching rather than on a run page nobody can read.
- **1.0.0.70** (2026-09-03): The .NET job went red on a commit that changed nothing but documentation, and there was no way to say which test without a GitHub sign-in, which is the third time a red job here has been unreadable. It names the failing test in annotations now, the same way the browser suite has since 1.0.0.54. And the benchmark that caused it is on its third design, because the first two were measuring the machine as much as the code: one timed each path five times in sequence, which compares two numbers taken at different moments on a shared machine, and the second alternated them but still took each path's own fastest, which lets one path's best come from a quiet moment and the other's from a busy one. It measures both paths inside each round now and compares them there, then takes the median of the five differences, so a round landing in a busy stretch has both its numbers inflated and the difference survives. Six runs came back between 17 and 56 ms in the index's favour, and a run with six processes deliberately burning the machine came back at 40 ms in its favour with every single round agreeing, where the old design had failed at 128 ms against it. The threshold has never been widened through any of this.
- **1.0.0.69** (2026-09-03): Reading the root directory, which no check had ever done, found two files the rename never touched because they were never about the name. WALKTHROUGH.md was the hiring challenge's own interview brief, sitting on a public repository explaining how a submission to somebody else's exercise would be evaluated. WORKING-NOTES.md was a hundred and eighty lines of my own notes from the first build day, addressed to myself, describing which parts of that challenge's identity had been scrubbed and how, and three days stale by its own numbers. The notes are kept and are not public any more; the brief is gone. And CLAUDE.md, the file an agent reads first to learn what it is working on, opened with "An industrial and farm equipment auction marketplace", a rename considered on day one and deliberately not done, left describing the version that never happened in the worst place in a repository for a sentence to be wrong. A test asserts that only the record explaining the history names the challenge, and that the two files saying what this project is agree about it. The coverage figures quoted in prose are dated now, because a number in a sentence goes stale silently and a dated one cannot.
- **1.0.0.68** (2026-09-03): How this was built now carries the objection it was missing. It already answered three a reviewer would raise about AI-written code, and the fourth is the strongest and was not there: an AI reviewing its own work is theatre. The answer is a list rather than an argument. The day's diff went to a reviewer with no memory of writing it, and two of its seven findings contradicted arguments made in the same commit, one of them a page explaining why a database parameter value must be structurally impossible to publish beside code putting the raw exception message on the same public page through another door. Then the shape of one finding was used as a search and it had two siblings, all three deliberate decisions defended in comments that were correct when written and left standing after accounts and a database arrived underneath them. What makes it evidence rather than theatre is what happened next: each fixed, each recorded with what it allowed rather than what mitigated it, each covered by a test that fails against the old behaviour, three of them fired at the live site to watch them fail there. The page also states the coverage rather than the test count, and the record count it had been quoting was nineteen records out of date.
- **1.0.0.67** (2026-09-03): Every record ends with links at the code it decided about, which is the main way a reader gets from a decision to the thing it decided, and nothing checked them. Three tests now do, and they found two: ADR-033 pointed at a migrations folder that stopped existing when the two-provider model moved the history into a project of its own, dead from that day until something looked, and one directory was linked with GitHub's file form, which works by redirect and stops working the moment somebody copies it somewhere that does not follow one. The link test insists on the right form for each, because a wrong link that redirects is a wrong link that will be right until it is not. The other two hold the records and the catalogue to each other in both directions, since a record nobody can open from the site only exists for whoever clones the repository, and a catalogue entry with no file behind it is a 500 waiting for a click, and assert the numbering runs without a gap, since a gap means a decision was made and then deleted, which is the one thing an index like this exists to keep.
- **1.0.0.66** (2026-09-03): A security page, which GitHub also picks up from docs/, saying what is protected and by what, what deliberately is not and why, and what went wrong. The third section is the useful one: a threat model with no findings in it has usually not been applied, and this one lists seven from a single afternoon with what each allowed and which record covers it. The pattern in the first three is the reason the page exists, because each was a deliberate decision defended in a comment, correct when written, and left standing after the assumption underneath it changed: accounts arrived and a database arrived and the sentences did not move, which is harder to catch than an absent thought because the file reads as though the question is settled. The habit it leaves is that when a system gains users or persistence, the comments saying "this is only a demo" are a work list.
- **1.0.0.65** (2026-09-03): This repository has been saying how many tests it has and nothing about what they touch, which is compatible with testing one class thoroughly and everything else not at all. coverlet has been a dependency of the test project since the project existed and nothing ever ran it, so the number cost one flag: 89.6 per cent of lines and 71.7 per cent of branches. The shape matters more than the total and it is the shape the architecture predicts, with the entities at 100, the domain at 98.8, the use cases and the adapters in the low nineties, and the host lowest at 78.2 because two of its classes talk to Azure with a managed identity and CI has no Azure credential and is never getting one; what is worth asserting about those two is that they degrade rather than throw without it, and that is tested. The floor is a ratchet rather than a target, set just under where the suites are, low enough that nobody is tempted to write a test which executes a line without asserting on it. Published as an annotation rather than only a job summary, because a summary renders for somebody signed in and this project has now paid twice for the lesson that a check nobody can read is not a check (ADR-051).
- **1.0.0.64** (2026-09-03): The same defect the review caught in one buffer was sitting in the one next to it. The middleware that catches every unhandled exception was recording its message into the ring served at /api/errors, unauthenticated, which is where a framework writes a filesystem path or the value that broke a constraint, and the ProblemDetails handler two regions away has refused to put an exception message in a response since the day it was written. It records the type now. Worth being precise about why it was missed: the review was scoped to a diff, /api/errors was older than the diff, and a review scoped to a change sees a change rather than a system, which is the wrong scope for a question of the form "can this kind of thing reach that kind of reader". And browser error reports have their own fifty slots rather than sharing the server's: the endpoint is anonymous on purpose so a crash in the page reaches the same place a crash in the server does, its message and stack were bounded but the number of reports was not, so fifty posts from anybody erased every real server error from the page an operator would open during an outage. The Admin tab still shows one list (ADR-045 addendum).
- **1.0.0.63** (2026-09-03): Login had a lockout policy on paper and none in practice. UserManager.CheckPasswordAsync verifies a password and touches nothing else, so AccessFailedCount was never incremented, no lockout ever engaged, and with no rate limiter in front of it the endpoint was an unmetered password oracle against real accounts: one request per guess, no delay, no ceiling. The AspNetUsers table has carried LockoutEnd, LockoutEnabled and AccessFailedCount since the schema was written, correctly typed and faithfully migrated and never once read. That is a more comfortable failure than a missing control, because everything about the system looks like it is there. Five wrong passwords now buy five minutes off, the lockout is checked before the password so a locked account stops answering guesses rather than merely stops confirming them, a success clears the count so a slow week of typos cannot lock somebody out of their own account, and the refusal says exactly what a wrong password says, because a distinct "this account is locked" is a reply confirming the address is registered here (ADR-050). And the search benchmark that failed at 243 ms against 115 ms was measuring unfairly rather than measuring a regression: it timed one path five times and then the other five times, which compares two numbers taken at different moments on a shared machine, and it passed six times of six alone five minutes later unchanged. The two paths take turns now. The threshold was not touched, because a measurement that is unfair is not fixed by widening what it allows.
- **1.0.0.62** (2026-09-03): An audit for other endpoints shaped like the reset found one worse. The simulated room's tick took no account at all and advanced state derived from everybody's bids, so a stranger with curl and a loop could counter-bid every auction any signed-in visitor was winning, move the prices shown on the public listing, and leave nothing behind that said who. The comment above it argued correctly that the room should answer a price rather than a person, and said nothing about who may advance it, because when it was written there was one visitor. What the room bids against is unchanged; moving it now costs a sign-in, the same as bidding, and the page stops asking when signed out rather than polling for a 401 forever, which means a signed-out visitor sees a still grid and that is the honest state (ADR-049). The same audit found the fix from one version earlier incomplete: it cleared the room's answers on every vehicle the caller had touched, and "vehicles this person bid on" is not "vehicles only this person bid on", so a reset still took away a stranger's outbid badge and dropped the price they were bidding against. The caller now clears the room only where nobody is bidding any more. Verified against the live site with two real accounts before the narrowing: one resets, the other keeps its bid, and the car still stands at their number.
- **1.0.0.61** (2026-09-03): The reset button deleted everybody's bids. The endpoint behind it took no user at all and the store's clear was a delete of the whole table, so any signed-in visitor could destroy every other visitor's rows, durably, on a site whose own changelog has said for twelve versions that two visitors can outbid each other and both be told the truth. The behaviour was deliberate and carried a comment defending it, written when a bid belonged to a browser and "everybody" meant "you", and the reason expired without the sentence changing, which is harder to catch than an absent thought because the file reads as though the question is settled. The reset belongs to the caller now, and the original reasoning survives narrowed to the vehicles they actually touched: their bids go, the room's answers on those same vehicles go with them, and each of those vehicles has its standing recomputed from whoever is left rather than deleted, so a stranger bidding on the same car keeps their bid and the lead they earned (ADR-048). Fixing it broke a test, which is the interesting part: smoke.spec reads the minimum next bid off a placeholder and posts it once with no retry, and it had been passing because market.spec's reset was clearing the simulated room globally, so there was nothing bidding against it. Scoping the reset left the room's other bids standing and the race that was always in that test started happening. A test that passes because another test keeps clearing the world is not a passing test, and it looks exactly like one until somebody fixes the thing it was leaning on. The bidding helper moved to its own module and both specs use it. And ADR-042 carries a correction: three ship attempts came back two green of three and that was attributed to the machine on real measurements, the suite having gone from 2.0 minutes to 3.4 while the .NET suite was unchanged. All true, and not the cause. Twelve consecutive green runs since the load wait started waiting for something that is there rather than something that is gone say the defect was the helper. A measurement that is real and an explanation that is true are different things.
- **1.0.0.60** (2026-09-03): The crawler files added one version earlier answered 404 on the live domain while the page that points at them carried every tag pointing there. The edge was ruled out in one line, because its rule is a catch-all proxy, which left the origin, which serves what the image contains, which is built from an explicit list of COPY lines in the Dockerfile that does not mention the public folder, because the folder did not exist when the list was written. The Dockerfile is a second manifest of what the frontend consists of: written in another file, maintained by hand, consulted by nothing until a deploy, and only ever evaluated in the one place the tests do not run, which is why local checks were green the whole time. A test now reads that stage of the Dockerfile and asserts every build input which exists in the repository is copied into it, and fails against the Dockerfile as it was (ADR-047).
- **1.0.0.59** (2026-09-03): The projects are named TheYard now. They were TheBlock, which is the name this application had when it was a submission to somebody else's hiring challenge, so for weeks the live site, the domain and the repository all said one thing and every project inside said another, and a reader who opened the solution found a different application than the front door advertised. Renamed with the history intact: the directories moved with git mv before a single byte of content changed, so all 208 files are recorded as renames and log --follow still works, and the content pass read each file as bytes and preserved its byte-order mark, so the diff contains only lines that actually mention the name. Four of the 921 references are strings rather than paths and would have failed silently at runtime instead of loudly at compile time: the migrations assembly Entity Framework is told by name, the DACPAC filename CI asserts, the source paths the live code samples read, and the container's entry point. The sweep also turned up an entire project directory, scaffolded during the SQL Server work and abandoned, in no solution and tracked by nothing, which no check in this repository could have found because nothing ever built it (ADR-046).
- **1.0.0.58** (2026-09-03): The page said nothing about itself to anything that was not a person. No description, no Open Graph, no structured data: a crawler, a chat client unfurling the link, a search engine and a recruiter's parser all read the head and nothing else, and the head held a title and a font. It carries a description sized to where it gets shown, an absolute preview image because a relative one is silently dropped, a canonical link, and structured data declaring what this actually is, which is source code by a named author rather than the Organization or Product that would look better and be false. There is a robots file that keeps crawlers off the endpoint which throws on purpose, and a sitemap, both of which work only because every view here is a GET URL. The preview card is drawn from the application's own palette with its numbers read from the repository, so it cannot claim a count the project no longer has, which the README had been doing: it said twenty-nine decision records and there were forty-five, sixteen records after that stopped being true, and a test now holds any number word in front of "decision records" to the real count. And ADR-001 ends by accepting a publicly reachable origin because it was "carrying no secrets and no persistent user data", which stopped being true when accounts arrived and the store moved to Azure SQL Database; the addendum says so, sets out what actually stands between the internet and that data today, and records that the origin lock is still owed rather than pretending an unfunded control is in place.
- **1.0.0.57** (2026-09-03): Fixing the timing section's status handling removed the only place a status was shown, which left the fix unverifiable from outside; the section reports answers by status now, an aggregate that names nobody, and a test hits the endpoint that throws on purpose and asserts it is counted as a 500, which is the assertion that pins the middleware above the exception handler where it belongs. The exclusion list is written out one endpoint at a time rather than matched on a prefix, because the deliberate-failure probe lives under the same prefix as the readers and is exactly the request the timing section should be showing.
- **1.0.0.56** (2026-09-03): The day's whole diff went to a reviewer that had not written any of it, and it found seven things. The one that mattered: the SQL section argues at length that a parameter value must be structurally impossible to publish, and the log section shipped in the same commit put the raw database exception message on the same public page, which is where a driver writes the server hostname, the login name and the caller's IP address. The health check two screens away refuses to do exactly that and says so in a comment written a week earlier. The exception travels in the exception slot now rather than inside a message template, so what reaches the console and what reaches a public page are different things on purpose, and the log ring is an allow-list of this application's own categories because a healthy container's framework lines announce its content root and its key directory. Second: the timing middleware sat below the exception handler and read the status before it was written, so every failed request was recorded as a 200, including the endpoint that exists to prove the failure path works. Also the connect timeout raised last version multiplied by a retry count nobody re-read, eight minutes against a five minute deploy; the metrics endpoint returned five hundred entries of what every other visitor was doing; the load helper asked for forty-five seconds inside a thirty second test and waited on an absence that was already true; the Admin tab filled its own buffers by being looked at; and a test passed only because of the order xUnit happened to pick. ADR-045 records all of it, including what the review looked for and did not find. And CI stopped being carried on borrowed compatibility: four actions were targeting Node 20 and being forced onto Node 24, and the runtime each major actually declares was read rather than guessed, which is how upload-artifact turned out to still be on Node 20 at v5.
- **1.0.0.55** (2026-09-03): The annotations added one version earlier paid for themselves on their first red run: CI had failed twice saying nothing but "exit code 1", and the third time it named the test, axe on the Admin tab, with three violations. Three is the number of scrollable table containers the SQL section added, and a box with overflow-x and nothing focusable inside is a region a mouse can scroll and a keyboard cannot, which is WCAG 2.1.1. It fires only when the box actually overflows, which is why a wide development window passed it and the runner's viewport did not, and why it was invisible to a local suite run eleven times. The three regions are focusable and labelled now.
- **1.0.0.54** (2026-09-03): A red browser suite on CI now names the tests it failed on, in annotations rather than a job summary, because the summary added one version earlier turned out to render only for somebody signed in to the repository while annotations come back from the public API and sit at the top of the run page. Two red builds in a row said nothing publicly except "Process completed with exit code 1" while their full evidence sat behind a sign-in, which is a poor place to keep the one thing a reader needs.
- **1.0.0.53** (2026-09-03): The deploy that failed while the site it was checking was serving perfectly well. Readiness had been answering "ready" only when every health check passed, and the database check joined that set when the relational store did, so a container running on the file-backed fallback reported itself unfit for traffic and `curl -fsS /readyz` failed the roll: the exact outcome ADR-033 exists to prevent, since the whole point of the fallback is that the site comes up when the database does not. Readiness and health are different questions now, and the database check is the one that answers the second without withholding the container from the first, with a test that names both halves. And CI can say what broke without a login: a red browser suite writes its own output onto the run page, because "Process completed with exit code 1" was the only public word on a failure whose full evidence sits behind a GitHub sign-in. And the cold-start fix from 1.0.0.51 turned out to be too narrow: it warmed the app before the first test, and the same failure came back in the middle of a run in a different spec, a focus test failing because no vehicle tile existed yet. Every spec here opens with a navigation and then asserts against a loaded app, so every one of those first assertions was carrying an unstated five-second load budget; the wait now lives with the navigation, once, and every navigation goes through it (ADR-042 addendum).
- **1.0.0.52** (2026-09-03): The Admin tab shows what the database is actually doing: every SQL statement Entity Framework sent, newest first, with the request that caused it, how long the database took, and each parameter listed by name, type and size. Not by value, and not by a redaction rule either, because a rule that lists what to hide is correct only for the columns that existed when somebody wrote it and this page is public: the type the table is built from has no field for a value, so a column added next year cannot leak through it, which a test proves by registering an account and asserting the address appears nowhere in the response while the statements do. Beside it the application's own log, and timing computed on read from the last five hundred requests, p50 and p95 per endpoint, exact for the window rather than approximate forever (ADR-043, and ADR-044 walks interceptors, why parsing the log line would have been worse, and the eleven lines a percentile actually takes). And the first roll on Azure SQL Database told us the last thing that was missing: the container reached the server, shook hands in 432 ms and authenticated in 4, then ran out of time in the post-login phase at 29.3 seconds against SqlClient's default 30, which is a paused serverless database waking up, so the connect timeout is ninety seconds now, set in code where the reason for the number can sit next to it.
- **1.0.0.51** (2026-09-03): The browser suite found a serious accessibility failure the moment it could report one: an auction that has ended paints its countdown in the palette's faint colour on the page ground at 3.82 against a required 4.5, and the palette test passed the whole time because it contained an exemption letting faint labels clear only 3:1 there on the reasoning that they live on white, which the stylesheet had never agreed with; the colour is deepened, the exemption is gone, and ADR-042 walks that and the four other checks that were asking an easier question than the one they were written for, among them a .NET test that asserted a diagram's title verbatim and had only ever passed because no title until now contained an apostrophe for the encoder to touch, and a keyboard test whose first line was quietly also asserting that a cold start finishes inside five seconds, which on one run in three with four workers arriving together it does not. The database has a picture now: an entity relationship diagram drawn from a table of facts rather than described in a code fence, showing all four tables this application owns with every column and the reason for its type, Identity's seven, and the two relationships the schema deliberately does not enforce, opening on its own zoomable page like every other diagram here; the infrastructure drawing has the database on it and the file-backed fallback beside it, having previously shown neither; and the documentation caught up with the code: the README described the store as SQLite and still listed durable storage across a container roll as something it would do with more time when that is exactly what shipped, and the two records that describe the store were still describing SQLite in the present tense and one of them was handing out a command that no longer works, so ADR-033 now says at the top that it is the decision to have a relational store and not a description of today's one, and ADR-034's schema-change steps name the SQL project and the migrations project that actually exist; and CI went red on the browser suite and could not say why, because the step that uploads a Playwright report has been finding nothing for as long as it has existed: without an html reporter that directory is never written, so every failing run on a machine nobody can log into left no evidence at all; the reporter is configured now, and the bid helper that made the suite deterministic on a fast machine waits for the server's answer rather than for six seconds of it, because a slow accept on a two-core runner was being read as a refusal and three of those threw.
- **1.0.0.50** (2026-09-03): 1.0.0.49 built the SQL Server backend and then shipped on SQLite, because the deploy asked Azure for the database server's address and the deploy's own identity is not allowed to read it, so the placeholder survived and the container correctly read a placeholder as no database; the address is composed from the name now, which is what it always was, and the deploy says out loud which store the container came up on rather than leaving it to whoever reads the health endpoint by hand (ADR-039 addendum).
- **1.0.0.49** (2026-09-03): The relational store is Azure SQL Database now, reached with no password at all: the server was created Entra-only so it has no SQL login to have one, and the container authenticates as the managed identity it already carried, which means the connection string in its environment is a server name, a database name and an authentication mode and nothing worth stealing; the schema itself moved out of C# into a SQL project that compiles to a DACPAC and is now the authority, with Entity Framework held to it by a conformance test and the running application stripped of the right to alter a table at all; and the schema is the current model done properly, with a length and a reason on every column, a decimal grade, a datetime2 auction start, a rowversion on bids, a real foreign key to the account and two relationships deliberately left unenforced, the first publish of which told us something no amount of C# would have: Identity's default 450-character keys put four primary keys over SQL Server's 900-byte clustered index limit (ADR-039, ADR-040, and ADR-041 walks it for a developer meeting either for the first time).
- **1.0.0.48** (2026-09-03): Bids belong to people now: accounts through ASP.NET Core Identity, a session that is a signed token in a cookie the page cannot read, and a bid keyed on the person as well as the vehicle, so two visitors on the live site can outbid each other and both be told the truth about it, with an account view that lists what you have bid on and whether you are still winning (ADR-037, and ADR-038 walks the setup for a developer meeting Identity and JWTs for the first time); and two flakes that predate this work went with it, one where the API tests bid on the auction closest to closing and lost it between reading the minimum and posting the bid about one run in three, the other where the search benchmark asserted an exact inequality under a doc comment claiming to be loose and failed at 95 ms against 73 ms with five suites running at once.
- **1.0.0.47** (2026-09-03): Photos are sized for the box they land in: the originals are 1280 wide and the card paints them at about 358, so a first load was pulling 2.3 MB to fill nine boxes and a phone was pulling exactly the same bytes as a desktop, and there is now a 480-wide copy of all fifty beside them, 91 per cent smaller each, chosen by the browser through srcset, with a test holding the photo manifest and the image directory to the naming the browser relies on (ADR-036).
- **1.0.0.46** (2026-09-03): The cheap half of what the review deferred, none of it a live defect and all of it the kind of thing that becomes one: the style job now actually runs before the other three, which its own comment had been claiming without a `needs` line to make it true; the test that holds a crash and a rejected query to one shape would have passed on two empty bodies and now asserts it compared something; the restart test matched a bid amount as a substring of a whole JSON document, where a thirteen-digit timestamp contains almost any five-digit number, and now parses the field; and the exception handler noticed that its writer can decline, which happens when a caller accepts only text, in which case the request is reset rather than answered and the log now says so (ADR-030).
- **1.0.0.45** (2026-09-03): The corrections from a staff review of the night's own diff, of which one was serious and visible: a collapsed sidebar hid thirty-five documents behind a control a mouse could not see, because a disclosure whose summary is screen-reader-only has no visible way to open it; the public health endpoint stopped repeating a storage failure's exception text, which is typically a filesystem path; bids are written to the store before memory so a failed write cannot leave a bid the caller was told had failed; an abandoned request answers 499 at Information instead of 200 at Debug, which is the difference between seeing an outage and not; and the accessibility scan now waits for the grid it is checking and opens the records index it was skipping (ADR-030, ADR-033, ADR-035).
- **1.0.0.44** (2026-09-03): axe now holds six views to WCAG 2.1 AA inside the browser suite, and its first run found two serious contrast failures on the two most visible elements on the page, the reserve chip at 4.39 and the live countdown at 4.04, in a repository whose own palette test was passing because it holds the pairs somebody listed while a stylesheet composes whatever it likes; both colours were deepened rather than the threshold lowered, and the palette test gained the two pairs it was missing (ADR-035).
- **1.0.0.43** (2026-09-03): The catalogue and the bids moved into SQLite through EF Core, with the schema applied by migration at startup and the tables seeded on first boot from the JSON files that used to be the catalogue, and not one line changed above the Infrastructure layer because both sources already sat behind ports; a bid now survives the process restarting, which a test proves by starting a second application against the same file, at a cost of about 1.4 seconds on a cold container and nothing at all on the path a request takes (ADR-033, and ADR-034 walks the setup for a developer meeting EF Core for the first time).
- **1.0.0.42** (2026-09-03): How this was built now sits in the About menu beside the README: it says outright that an AI agent wrote most of this application, names the criteria a reviewer should judge that by, points each one at a place in the repository, and gives most of its length to the times the work was wrong, including the three objections a reader would raise and what the counter-evidence actually is (ADR-032).
- **1.0.0.41** (2026-09-03): The version comes from this file's top line now rather than from the deploy's run counter, after a red build consumed run number 28 and left the footer saying 1.0.0.40 while the line written with it said 1.0.0.39, so a ship that forgets its changelog line fails instead of shipping a number nothing describes (ADR-031); and the failure nobody wrote code for finally answers properly, with a handler that keeps the message and the stack inside, returns the same problem shape as a rejected query, and an endpoint that throws on purpose so that path can be exercised in production instead of assumed (ADR-030).
- **1.0.0.40** (2026-09-03): Style stopped being a preference: four checks now fail the build (dotnet format, Prettier, oxlint, and the typecheck that was already there), each proven to fail on bad input, and the linter's first run found a derive-do-not-store violation in the component that serves the style document (ADR-028); the twenty-nine records moved out of four topic sections into one numbered index that opens on demand (ADR-029); the first CI run of the new style job then failed on the command that had just passed locally, which turned out to be the repository claiming CRLF line endings while storing LF, true on Windows and false on the Linux runner, now fixed in .gitattributes and enforced by both formatters (ADR-028).
- **1.0.0.38** (2026-09-03): Two screenshots of the room answering a bid on the live site, and the defect the first of them caught: for the moment between a competing bid and the browser refetching, the panel offered a minimum equal to the standing bid, which is a number the server would have refused (ADR-027).
- **1.0.0.37** (2026-09-03): The auction has other bidders now: a simulated room answers the buyer's bids through the same BidRules the buyer's bids go through, waits before answering, stops at twice the opening ask and never takes a vehicle at buy-now, so the high-bidder badge can come off and the minimum next bid climbs the way an auction's does, shipped after a self review that found and fixed eleven defects in it, four of them serious enough to be worth reading about in the record (ADR-027).
- **1.0.0.36** (2026-09-03): The keyboard path is walkable end to end: a skip link past the thirty-button rail, focus that moves to the view that changed instead of falling to the document body, and a live region that names where you arrived, all four held by Playwright (ADR-026).
- **1.0.0.35** (2026-09-03): Free-text search stopped rebuilding each vehicle's searchable text inside the scan, which halves the work across the hundred thousand rows and is measured by a test that ships with it (ADR-025), the telemetry query that shipped broken in 1.0.0.34 now parses and the Admin card counts browser errors beside the server's (ADR-024), and the em dashes are gone from the code comments the records display.
- **1.0.0.34** (2026-09-03): Application Insights is wired: every request, dependency and exception the API handles goes to it, browser errors ride the endpoint that already existed, and the Admin tab reads the last hour back with the container's own identity, with the ingestion key read from Azure at roll time and never stored in the repository (ADR-024).
- **1.0.0.33** (2026-09-03): The changelog was corrected after two commits shipped in one deploy run: they became one version, not two, and this line records the fix (ADR-012).
- **1.0.0.32** (2026-09-02): Every served document was audited against the code in six classes and the twelve claims that had drifted were corrected, the largest being a record still describing the old 400 body, and the nine teaching comments a staging bug had dropped were put back (ADR-017 addendum).
- **1.0.0.31** (2026-09-02): Every code sample a record shows now carries a teaching comment explaining why it is written that way, so a reader meeting the pattern for the first time learns from it (ADR-006 addendum).
- **1.0.0.30** (2026-09-02): The README was rewritten to what is live now, with all four "with more time" promises closed and what remains listed honestly, and the last two documents carrying em dashes from before the voice rule were rewritten.
- **1.0.0.29** (2026-09-02): Every failure now answers one shape, RFC 9457 ProblemDetails with the message in detail and a trace identifier, every request is logged as structured JSON, and a React error boundary turns a render crash into a page with a way out and reports it to the Admin tab (ADR-023).
- **1.0.0.28** (2026-09-02): The sidebar gained an App Architecture section holding the new architecture and style pages, the data flow, the project structure and the three records that walk the code (ADR-022).
- **1.0.0.27** (2026-09-02): The third record for a new developer, the tests explained, with teaching comments in the samples it shows, and the resume dated 2026-09-02 served from About (ADR-021).
- **1.0.0.26** (2026-09-02): ADR: Diagram pages gained its screenshots from the live site, the caption on a phone, the page as it opens and zoomed in, and the laptop view (ADR-020).
- **1.0.0.25** (2026-09-02): Every diagram opens on its own page in a new tab, zoomable with the text selectable, from a preview in the record; the data flow was drawn in the same style as the infrastructure diagram; links inside a document open in a new tab (ADR-020).
- **1.0.0.24** (2026-09-02): Two records written for a new developer walk Program.cs and the React configuration top to bottom with the why beside every choice and the code read from the build, and a live sample can now show a whole file (ADR-018, ADR-019).
- **1.0.0.23** (2026-09-02): The Hosting page and the README gained an infrastructure diagram, the records gained screenshots from the live site and more live code, and the documenting rule was written down (ADR-006 addendum).
- **1.0.0.22** (2026-09-02): A staff-level review of the day's work landed its corrections (one documents endpoint over a catalog, honest failure states on the Admin tab, the phone header's way home from Admin) and every decision record gained a map of the files it decided (ADR-017).
- **1.0.0.21** (2026-09-02): The whole site repainted to Figma's Urban slate, gray, brown and blue, with every text and ground pair measured against WCAG AA (ADR-016).
- **1.0.0.20** (2026-09-02): Cache headers arrived: hashed bundle files cache for a year and everything else says no-cache, so a new version shows on the next load on any device (ADR-015).
- **1.0.0.19** (2026-09-02): The sidebar turned light and modern, a white rail and drawer with the brand blue as the only accent (ADR-013 addendum).
- **1.0.0.18** (2026-09-02): The CI/CD page gained its Deploy screenshots, the six green runs and one run's steps, captured from the public Actions pages.
- **1.0.0.17** (2026-09-02): The Admin tab's health checks report how long each one took and Azure's card lists the container's last three events (ADR-010, second pass).
- **1.0.0.16** (2026-09-02): Decision records began showing live code samples read from the running build's own source, with that source shipped inside the image (ADR-014).
- **1.0.0.15** (2026-09-02): The five header dropdowns gave way to one sidebar at every width, a docked rail on laptops and the drawer on phones (ADR-013).
- **1.0.0.14** (2026-09-02): A Changelog menu joined the header, and the phone menu became a dark slide-out panel with an icon on every row.
- **1.0.0.13** (2026-09-02): The phone header shipped as the first real passenger through the new pipeline, alongside the pipeline's own record (ADR-011).
- **1.0.0.12** (2026-09-02): The first automated deploy: the CD pipeline shipped itself and rolled this version with no hands (ADR-009).
- **1.0.0.11** (2026-09-01): The Admin tab was roughed in with health checks, the error buffer, and Azure's own view of the container (ADR-010).
- **1.0.0.10** (2026-09-01): The Netlify credit fix landed with two lesson records, edge deploy economics and Linux over Windows.
- **1.0.0.9** (2026-09-01): The version footer arrived and the page started reporting its own build (ADR-005, ADR-006).
- **1.0.0.8** (2026-09-01): The About menu began serving the current resume.
- **1.0.0.7** (2026-09-01): The README was reframed on the project's own terms and its typography restored after the encoding damage.
- **1.0.0.6** (2026-09-01): The production design was documented as deliberately undeployed, and the bare domain got its edge rules.
- **1.0.0.5** (2026-09-01): The Hosting overview's child index gave way to the websites-and-resources list after a proofread.
- **1.0.0.4** (2026-09-01): Configuration screenshots were embedded in the docs, the CI/CD menu was seeded, and the Bicep file started serving from the dropdown.
- **1.0.0.3** (2026-09-01): Hosting became the parent record, with the ADRs nested under it in a new Hosting menu.
- **1.0.0.2** (2026-09-01): The domain docs shipped and the ADRs went live in the About menu behind https://theyard.stevenstout.biz.
- **1.0.0.1** (2026-08-31): The first live deploy, Azure Container Instances over plain HTTP on port 8080, the day the site got a URL.
